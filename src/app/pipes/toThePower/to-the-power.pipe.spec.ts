import { ToThePowerPipe } from './to-the-power.pipe';

describe('ToThePowerPipe', () => {
  it('create an instance', () => {
    const pipe = new ToThePowerPipe();
    expect(pipe).toBeTruthy();
  });
});
